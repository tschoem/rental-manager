import PropertyForm from "../_components/PropertyForm";

export default function NewPropertyPage() {
    return (
        <div>
            <h1 style={{ fontSize: '2rem', marginBottom: '2rem' }}>Add New Property</h1>
            <PropertyForm />
        </div>
    );
}
